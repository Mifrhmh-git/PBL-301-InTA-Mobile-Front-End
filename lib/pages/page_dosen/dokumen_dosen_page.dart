import 'package:flutter/material.dart';

class DokumenDosenPage extends StatelessWidget {
  const DokumenDosenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dokumen Dosen')),
      body: const Center(child: Text('Halaman Dokumen Dosen')),
    );
  }
}

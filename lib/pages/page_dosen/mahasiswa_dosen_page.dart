import 'package:flutter/material.dart';

class MahasiswaDosenPage extends StatelessWidget {
  const MahasiswaDosenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mahasiswa Bimbingan')),
      body: const Center(child: Text('Halaman Mahasiswa Dosen')),
    );
  }
}
